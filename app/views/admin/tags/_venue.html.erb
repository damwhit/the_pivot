<tr>
  <td><%= venue.name %></td>
  <td class="col-md-2">
    <%= link_to "remove #{tag.name} tag", admin_venue_tag_path(venue.id, tag.id), method: "delete", class:"btn btn-danger btn-s text-center" %>
  </td>
</tr>
